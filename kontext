#!/bin/sh
# ~z

source ks_config.sh

select context in 'dev-telhub' 'uat-telhub' 'prf-telhub' 'prd-telhub' "$CONTEXT"
do
       echo $context;
       kubectl config use-context $context;
       exit;
done
